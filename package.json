{
  "dependencies": {
    "vanilla-clipper": "^0.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "vanilla-clipper": "./node_modules/.bin/vanilla-clipper"
  }
}
